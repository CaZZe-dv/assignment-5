name: untitled
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.1.5
  shared_preferences: ^2.0.15
  provider: ^6.1.0
  geolocator: ^9.0.2
  flutter_local_notifications: ^14.0.0
  geocoding: ^2.1.0  # Add geocoding dependency
  location: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  # Uncomment and add your assets if needed
  # assets:
  #   - images/profile_picture.png
